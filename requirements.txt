# Python packages needed to build and publish ossum.tech.
#
# CI installs these, then `pip install -e .` for the RIDDL Pygments lexer.
# Keep to the community edition of mkdocs-material: a local machine may have
# Insiders installed, but CI does not, so Insiders-only features must not be
# used in mkdocs.yml.

mkdocs-material>=9.5
mike>=2.1

# Cross-site search. Pagefind indexes BUILT HTML, which is why it can span the
# four separate MkDocs projects that Material's own per-build search cannot.
#
# The [bin] extra is REQUIRED: the bare `pagefind` package is only the Python
# API wrapper and raises "Could not find pagefind binary" at run time.
pagefind[bin]>=1.5
